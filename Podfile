# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Rocket launcher' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Rocket launcher
  	pod 'SDWebImage', '~> 5.0'
    pod 'SVProgressHUD'
    pod 'PromiseKit'

  target 'Rocket launcherTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
